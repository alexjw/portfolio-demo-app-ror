<tr class="nested-fields">
  <td><%= f.collection_select(:ingredient_id, Ingredient.all, :id, :name, { prompt: 'Select' }) %></td>
  <td><%= f.text_field :quantity %></td>
  <td><%= link_to_remove_association "Remove Ingredient", f %></td>
</tr>
